image: node:22.11.0

stages:
  - build
  - deploy

cache:
  key:
    files:
      - package-lock.json
    prefix: npm
  paths:
    - node_modules/

build:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - dist/
  except:
    - tags  # No need to rebuild for npm deployment

pages:
  stage: deploy
  script:
    - cp -a dist/. public/
  artifacts:
    paths:
      - public
#  rules:
#    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

deploy-npm:
  stage: deploy
  image: node:20
  script:
    - "sed -i -e 's/^  \\\"version\\\".*/  \\\"version\\\": \\\"'${CI_COMMIT_REF_NAME/v/}'\\\",/g' package.json"
    - npm config set legacy-peer-deps=true
    - CI=true npm install
    - CI=true npm run build
    - npm config set '//registry.npmjs.org/:_authToken' "$NPM_AUTH_TOKEN"
    - CI=true npm publish --access public
  only:
    - tags
